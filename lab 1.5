#include <iostream>
#include <math.h>
using namespace std;

void main()
{
		cout << "Enter two digits" << endl;
		cin >> a >> b;
		a = abs(a);
		b = abs(b);
		cout << a + b << endl << a - b << endl << a * b << endl << a / b << endl;
}
