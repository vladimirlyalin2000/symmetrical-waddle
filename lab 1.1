#include <iostream>
using namespace std;

void main()
{
    float a, b, p, s;
    cout << "Enter A and B" << endl;
		cin >> a >> b;
		p = (a + b) / 2;
		s = a * b;
		cout << "P = " << p << endl << "S = " << s << endl;
}
